class server:

     def __init__(self):
            print (Fore.WHITE + "Welcome")
   
     def ip (self, ip):
            self.ip = ip
            print ("The ip is", ip)

     def port (self, port):
            self.port = port
            print ("The port is", port)
