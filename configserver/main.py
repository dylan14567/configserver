class server:

     def _init_(self):
            print (Fore.WHITE + "Welcome")
   
     def ip (self, ip):
            self.ip = ip
            print ("The ip is", ip)

     def port (self, port):
            self.port = port
            print ("The port is", port)
